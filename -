import { Component, Input } from '@angular/core'
import { User } from 'src/app/interfaces/user'
declare const window: any

@Component({
  selector: 'app-modal-trasacion',
  templateUrl: './modal-trasacion.component.html',
  styleUrls: ['./modal-trasacion.component.scss']
})
export class ModalTrasacionComponent implements OnInit {
  constructor () { }

  @Input() titulo!: string
  @Input() usuario!: User
  @Input() total!: number
  @Input() nip!: number
  public nipvalid: boolean = true

  public trasacion (): void {

  }
}
